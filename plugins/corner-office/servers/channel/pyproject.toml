[project]
name = "corner-office-channel"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.26.0",
    "websockets>=16.0",
]

[project.scripts]
corner-office-channel = "server:main"

[tool.setuptools]
py-modules = ["server"]
